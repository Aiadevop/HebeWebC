export const PublicRoute = ({children}) => {

    return children;

    // const {logged} = useContext(AuthContext)

    // return (!logged)
    //     ? children
    //     :<Navigate to="/login" />

}
